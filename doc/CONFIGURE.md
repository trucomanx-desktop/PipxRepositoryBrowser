# pipx-repository-browser

Browse curated Python package repositories and manage installations with pipx.

# Configure

Go to `Configure` to open the `~/config/pipx_repository_browser/config.json` file. 

